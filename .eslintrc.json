// extends next enables rules for Next.js
// plugin:prettier enables prettier integration in eslint rule & turns off any eslint rules that would conflict with prettier

{
  "extends": ["next", "plugin:prettier/recommended"],
  "rules": {}
}
