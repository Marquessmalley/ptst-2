import { BookingStepper, StepperProgress } from "@/components/booking";

const page = () => {
  return (
    <div>
      <StepperProgress />
      <BookingStepper />
    </div>
  );
};

export default page;
