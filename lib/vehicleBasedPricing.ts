import { detailMenu } from "@/lib/placeholder-data";

// vehicle-based dynamic pricing
export const vehicleBasedPricing = (selectedVehicle: string) => {
  //  Needs to return a new array based on selected vehicle
  // filter the vehicleType with only the specific vehcile type
  // console.log(selectedVehicle);
  // console.log(detailMenu);
};
